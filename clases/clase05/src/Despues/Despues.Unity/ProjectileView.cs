using UnityStubs;

namespace Despues.Unity;

// Marca visual simple: la lógica real de daño vive en el dominio (RangedAttack),
// esta clase solo existe para tener "algo" que instanciar en el motor, igual
// que Antes.Projectile.
public class ProjectileView : MonoBehaviour
{
}
